# JobTread Pave API (server-side only — never expose to the browser)
JT_GRANT_KEY=
JT_ORG_ID=22PXG7QcMaQ2

# Gemini (invoice extraction — powers the Add a Bill screen).
GEMINI_KEY=
# GEMINI_MODEL=gemini-2.5-flash   # optional; defaults to the Apps Script engine's model

# Claude (the /chat assistant, and the Admin Daily Digest's summary paragraph +
# email-signals extraction — one call each, per digest run. Unset and the
# digest still works, it composes its own plain summary instead.)
ANTHROPIC_API_KEY=
# Model for the /chat assistant. Defaults to Sonnet 5, the cost/speed pick for
# this high-volume chat surface.
ANTHROPIC_MODEL=claude-sonnet-5
# ANTHROPIC_MODEL_DIGEST=claude-sonnet-5   # optional; digest summary + email-signal extraction. Defaults to Sonnet 5.

# JobTread vendor mapping — Sunset's JT ACCOUNT id (Sunset bills keep their
# arrival date as the JT issue date; everyone else gets last-day-of-billing-month)
JT_SUNSET_VENDOR_ID=

# Ascent's own ACH numbers, for the copy chips on /payments (the TSYS payment
# form asks for them on every statement). Server-only — read by
# /api/bank-details, which is gated to ADMINS. Never prefix these NEXT_PUBLIC_.
# Leave blank and the row simply doesn't render.
ASCENT_BANK_ROUTING=
ASCENT_BANK_ACCOUNT=
ASCENT_BANK_LABEL=

# Auth allowlist (comma-separated Google accounts)
ALLOWED_EMAILS=office@ascentbuildingco.com,keillor@ascentbuildingco.com

# Master switch for ALL writes to JobTread.
#
# ⚠️ IN PRODUCTION THIS IS ARMED ("true") and has been for some time. The bills,
# coding, invoicing, time entries and leave posting in the live app are all real
# writes. Do not read the `false` below as a description of production — it is a
# deliberately SAFE LOCAL DEFAULT, because a dev machine points at the same live
# JobTread org, and an armed local instance would write to real jobs.
#
# (The old comment here said to leave it off "until coordinated with the
# AppSheet flow". AppSheet was retired 2026-07-10; that precondition is long
# since met. Leaving the stale note in place had every fresh session concluding
# writes were off.)
#
# Leave false locally unless you specifically intend to write to live JobTread.
COMPANION_WRITES_ENABLED=false

# Apps Script web app (Sheets/Drive-backed features: employees, tools, safety
# meetings, email logging, mileage). The Assistant has no Sheets client, so it
# POSTs {action, secret, ...} to this versioned /exec URL. The secret must equal
# Script Property SYNC_TRIGGER_SECRET on the Apps Script side.
APPS_SCRIPT_SYNC_URL=
APPS_SCRIPT_SYNC_SECRET=

# Google Maps Platform key for the Mileage tracker — used server-side by
# /api/mileage to turn the trip's start/end GPS points into driving miles + the
# street addresses. Enable the **Routes API** (driving miles) on the project;
# for the start/end addresses also enable the **Geocoding API** (optional —
# miles still work without it). NOT the legacy Directions API. Usage is tiny
# (a couple of calls per logged trip), well under the free credit.
GOOGLE_MAPS_API_KEY=

# Assistant database (non-billing data like RFIs). Local file by default; point
# at a hosted libSQL/Turso URL for deploy (with DATABASE_AUTH_TOKEN).
DATABASE_URL=file:./data/companion.db
# DATABASE_AUTH_TOKEN=

# Shared-password auth (fallback / transition). Unset = OFF for local dev.
# APP_PASSWORD=

# Google sign-in (Auth.js). Google is used when AUTH_GOOGLE_ID is set; otherwise
# the app falls back to APP_PASSWORD. Create an OAuth client in Google Cloud with
# redirect URI https://<your-app>/api/auth/callback/google.
# AUTH_SECRET=            # random 32+ byte string (npx auth secret, or openssl rand -base64 32)
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=
# ALLOWED_EMAILS=office@ascentbuildingco.com,keillor@ascentbuildingco.com

# Error monitoring (Sentry). ENTIRELY OPTIONAL — leave every value unset and the
# SDK is inert: no reporting, no network calls, and the build is unaffected. That
# is the supported state for local dev, CI, and a fresh clone.
#
# To turn reporting on, set just the DSN (it is a write-only ingest endpoint, so
# it is safe to expose to the browser — that is why it is NEXT_PUBLIC_):
# NEXT_PUBLIC_SENTRY_DSN=https://<key>@<org>.ingest.sentry.io/<project>
#
# The three below are ONLY for uploading source maps, which turns minified
# production stack traces back into readable file/line references. Without them
# the build still succeeds and errors are still reported — just harder to read.
# SENTRY_ORG=
# SENTRY_PROJECT=
# SENTRY_AUTH_TOKEN=

# Admin Daily Digest — the shared secret the daily scheduler uses to trigger a
# digest run. /api/digest/run is the one route that skips the Google session
# check (a cron has no session), so it authenticates on this instead: a caller
# must send `Authorization: Bearer <this value>` OR be signed in as an admin.
#
# On Vercel: set CRON_SECRET on the project and Vercel Cron sends that header
# automatically to the schedule in vercel.json (13:00 UTC = 6am Pacific). This
# variable is the override if you'd rather keep the digest's secret separate.
# UNSET = only a signed-in admin can trigger a run; the cron will get a 401.
# DIGEST_CRON_SECRET=
